apiVersion: v1
kind: Service
metadata:
    name: simplonalfredazurevotingappredis
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: simplonalfredazurevotingappredis